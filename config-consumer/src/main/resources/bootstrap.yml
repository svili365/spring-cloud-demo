server:
  port: 9091
spring:
  cloud:
    config:
     uri: http://localhost:9090
     profile: dev
     label: master
  application:
    name: config-consumer
  boot:
    admin:
      url: http://localhost:8080 #server端地址
    sleuth:
      sampler:
        percentage: 0.05  #采样率